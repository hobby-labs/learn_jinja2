# Command

```
docker run --volume ${PWD}:/opt/learn_jinja2 -ti python:3 bash

cd /opt/learn_jinja2
python -m venv venv/
source venv/bin/activate

pip install --upgrade pip
pip install -r requirements.txt
```

